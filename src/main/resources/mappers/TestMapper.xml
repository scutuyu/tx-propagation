<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tuyu.dao.TestMapper">

    <update id="a">
        update `name` set  `name` = concat(`name`, 'new') where id = 1
    </update>
    <update id="b">
        update `name` set  `name` = concat(`name`, 'new') where id = 2
    </update>

</mapper>